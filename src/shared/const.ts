/**
 * A constant string used to denote the output of a command.
 */
export const COMMAND_OUTPUT_STRING = "Output:"

/**
 * The maximum number of images allowed per message.
 *
 * @remarks
 * This constant is set to 20 as per Anthropic's limitations on the number of images that can be included in a single message.
 */
export const MAX_IMAGES_PER_MESSAGE = 20
